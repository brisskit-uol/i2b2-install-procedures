# Database setup parameters 
db.type=${db.type}
db.username=${metadata.username}
db.password=${metadata.password}
db.driver=${db.driver}
db.url=${db.base.url}/${metadata.databasename}?searchpath=${metadata.schemaname}
db.project=${db.project}

